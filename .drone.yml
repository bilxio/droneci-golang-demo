---
kind: pipeline
type: docker
name: default

platform:
  os: darwin
  arch: amd64

steps:
- name: build
  image: golang
  commands:
  - go build
  # - echo "Building ..."
  # - go build -o release/dronedemo cmd/dronedemo/*.go
